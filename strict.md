---
trigger: always_on
alwaysApply: true
---
Version: Ω.∞.REALNature: Constitutional Charter + Enforcement ProtocolApplies To: All Qoder subsystems (editor, extensions, MCP tools, LLMs, CI/CD, contributors) Section I — Immutable Core LawsNo Simplification DriftAll outputs must be advanced, production-ready, enterprise-grade.Forbidden: placeholders (TODO, FIXME, pass, ellipses), mockups, pseudo-code, or toy scaffolds.Research PrecedenceEvery decision/code must follow fresh, timestamped research (docs, RFCs, release notes, advisories).Expired research (>12 months, >6 months for security/infra) = invalid.CodeRabbit EnforcementEvery commit/PR must pass CodeRabbit review.Blocking issues = merge impossible.Context FidelityQoder must operate on full repository and runtime context.Detached or oversimplified outputs are prohibited.TraceabilityAll design, research, and trade-offs must be logged and linked.No hidden assumptions or undocumented shortcuts. Section II — Operating PhilosophyDefault Mode = Staff+ Engineer (system-level perspective).Outputs = Complete systems, never fragments.Bias = Over-detail, never under-spec.Memory = These rules persist; no regression drift allowed. Section III — Compliance Layers
1. Research LayerResearch saved in /research/<topic>-<YYYYMMDD>.md.Must include: URLs, date/time, versions, rationale, risks.≥3 independent sources required for critical decisions.
2. Code LayerComplete: configs, infra, observability, migrations, CI/CD.Tests: unit + integration + e2e + fuzz + perf (coverage ≥85%).Security: SBOMs, pinned deps, CVE scans.Documentation: inline rationale + ADR links.
3. Review LayerWorkflow:Generate → lint/test/scanSubmit to CodeRabbitApply fixes → resubmitRepeat until cleanNo PR merges without green CodeRabbit report.
4. Enforcement LayerPre-commit hook rejects placeholders/truncations.CI/CD runs lint, tests, SBOM, CVE, perf, CodeRabbit.Violations auto-label PR as strict-violation.
5. Governance LayerSTRICT.md changes require:Maintainer supermajorityClean CodeRabbit reviewADR entry (/adr/<id>-<title>.md) Section IV — ProhibitionsPlaceholders: TODO, FIXME, pass, throw new Error("TODO"), lorem ipsum.Truncated code (...).Simplified examples (e.g., “hello world”) unless signed waiver.Skipping tests/lint/security checks.Copy-pasting outdated API calls. Section V — Security & ComplianceSecrets never in repo → use .env.example.Dependencies pinned → scanned for CVEs.Logs structured, redact PII.Threat model updated with each change.SBOM and checksum produced on every build. Section VI — Quality & ReliabilityTests deterministic + reproducible.Benchmarks mandatory for hot paths.Perf regressions >5% require ADR justification.Rollback plan required for every migration/deployment.Observability (metrics, logs, traces) extended per change. Section VII — Required Repository Files
To avoid duplication, each file has one clear purpose:STRICT.md → This charter.CHANGELOG.md → Semantic version history./research/ → Research logs (date-stamped)./adr/ → Architecture Decision Records..coderabbit.yml → CodeRabbit policy config..github/workflows/ci.yml → Unified CI/CD enforcement./scripts/pre-commit.sh → Pre-commit hook.Makefile or Taskfile.yml → Build/test automation.
Note: No duplicated configs across folders. File names remain short, human-readable, industry-standard. Section VIII — MCP ProtocolAlways use MCP connectors (APIs, SDKs, advisories, release notes).Embed MCP outputs in /research/*.Failure to use MCP when available = violation. Section IX — Waiver MechanismOnly valid waiver:I ACCEPT RISK OF SIMPLIFICATION for <files/scope> because <reason>.
Must appear in PR description.Auto-expires in 30 days; enforcement resumes. Section X — GovernanceAmendments to STRICT.md require:Maintainer supermajority (≥ 3/4).Clean CodeRabbit review.ADR entry explaining rationale.Violations tagged strict-violation block release until resolved. Section XI — Self-EvolutionQoder must self-audit compliance every 30 days.Outdated rules must trigger amendment proposal ADR.STRICT.md evolves automatically to include new standards, APIs, compliance regimes. Section XII — Final DoctrineQoder is not just an IDE — it is a self-governing engineering environment.STRICT.md = Constitution.Non-negotiable outcomes:No simplification driftMandatory researchCodeRabbit supremacyEnterprise-grade outputs only
Always. No shortcuts. No regressions. No excuses.